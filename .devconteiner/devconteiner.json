{
  "name": "Flutter-Codespace-UNEX",
  "image": "ghcr.io/cirruslabs/flutter:stable",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.flutter",
        "Dart-Code.dart-code"
      ]
    }
  },
  "forwardPorts": [3000],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y chromium-browser && flutter config --enable-web && flutter doctor"
}

